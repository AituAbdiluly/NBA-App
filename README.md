# NBA-App
### NBA app with ReactJS.

### Installation

Install the dependencies.

```sh
$ npm install
```

#### Run the project:

```sh
$ npm start
```
And same time it needs to run json-server:

```sh
$ npm run dev-serv
```
